<button catchtap="onTap" type="default" hover-class="none"
class="{{state === 'on' ? 'on' : 'off'}}">
  <text wx:if="{{state !== 'waiting'}}" 
  class="{{state === 'on' ? 'fa ' + iconOn : 'fa ' + iconOff}}">
  </text>
  {{label}}
</button>